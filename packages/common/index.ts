export * from './src'

// import * as dev from './src'
// import * as dist from './dist'

// let module: typeof dev | typeof dist | {} = {}

// if (process.env.NODE_ENV === "UNITTEST") {
//   module = dist
// } else {
//   module = dev
// }

// export default { ...module }
